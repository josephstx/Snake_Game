#pragma once

enum class Scenes {
    Menu,
    Game,
    Scores, 
    Exit
};

enum class Dirs {
    Up,
    Down,
    Left,
    Right,
};

enum class Sounds {
    Eat,
    Menu,
    Start,
    GameOver
};